<div class="bg-gray-900 min-h-screen text-white font-sans">
    <script src="https://cdn.tailwindcss.com"></script>

    <!-- Toast Notification HTML -->
    <div id="toast-container" class="fixed top-0 left-1/2 -translate-x-1/2 mt-4 z-50">
        <div id="toast-alert" class="hidden max-w-sm w-full rounded-xl shadow-lg p-2 transition-all duration-300">
            <div class="flex items-center space-x-3">
                <!-- Success Icon -->
                <svg id="toast-icon-success" class="h-8 w-6 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                </svg>
                <!-- Error Icon -->
                <svg id="toast-icon-error" class="hidden h-8 w-6 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                </svg>
                <div class="flex-1">
                    <p id="toast-message" class="text-sm font-extrabold"></p>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Custom Modal for Remove Confirmation -->
    <div id="remove-modal" class="fixed inset-0 bg-gray-900 bg-opacity-75 flex items-center justify-center z-50 hidden">
        <div class="bg-gray-800 p-8 rounded-lg shadow-xl max-w-sm w-full text-center">
            <h4 class="text-xl font-bold mb-4">Remove Item?</h4>
            <p class="text-gray-400 mb-6">Are you sure you want to remove this item from your cart?</p>
            <div class="flex justify-center gap-4">
                <button id="cancel-remove-btn" class="px-6 py-2 rounded-lg text-gray-300 bg-gray-700 hover:bg-gray-600 transition-colors">Cancel</button>
                <button id="confirm-remove-btn" class="px-6 py-2 rounded-lg text-white bg-red-600 hover:bg-red-500 transition-colors">Remove</button>
            </div>
        </div>
    </div>

    <!-- Breadcrumb -->
    
    <div class="px-6 md:px-12 py-4 text-gray-500 text-sm">
        <a href="/" class="hover:text-white transition-colors">HOME</a> <span class="mx-2">›</span> CART
    </div>

    <div class="px-6 md:px-12 py-8 flex flex-col lg:flex-row gap-12">
        <% if (cartItems.length > 0) { %>
            <div class="flex-1">
                <h1 class="text-3xl font-bold tracking-tight text-white mb-2">Your Cart</h1>
                <div class="h-0.5 w-32 bg-purple-900 rounded-full mb-8"></div>
                
                <div class="space-y-6">
                <% cartItems.forEach(item => { %>
                    <div class="flex items-center bg-gradient-to-br from-gray-900 to-gray-600 rounded-xl p-4 transition-all duration-300 hover:shadow-lg hover:border-purple-500/50  <%= item.isAvailable ? '' : 'opacity-50' %>">
                        <img src="<%= item.image %>" alt="<%= item.name %>" class="w-24 h-32 object-cover rounded-lg" />
                        
                        <div class="flex-grow px-6">
                            <h3 class="text-lg font-semibold text-white"><%= item.name %></h3>
                            <p class="text-sm text-gray-400 mt-1">Size: <%= item.size %> / Color: <%= item.colorName %></p>
                            <% if (!item.isAvailable) { %>
                                <p class="text-red-500 font-semibold mt-2">Currently Unavailable</p>
                            <% } %>
                        </div>

                        <div class="flex flex-col items-end gap-4">
                            <div class="text-right mr-12 w-32">
                              <% if (item.bestOffer) { %>
                                 <span class="text-3xl font-bold text-red-500">₹<%= item.finalPrice.toLocaleString() %><span class="text-green-500 text-xs">(<%=  item.bestOffer.discountPercentage %>%OFF)</span></span>
                                 <s class="text-2xl font-bold text-gray-200 mr-12 block">₹<%= item.originalPrice.toLocaleString() %></s>
                              <% } else { %>
                                <span class="text-3xl font-bold">₹<%= item.originalPrice.toLocaleString() %></span>
                              <% } %>
                            </div>
                            <div class="flex items-center gap-2 <%= item.isAvailable ? '' : 'pointer-events-none' %>">
                                <button
                                    class="decrease-quantity-btn w-8 h-8 rounded-full flex items-center justify-center bg-gray-700 text-lg text-white hover:bg-indigo-600 transition-colors"
                                    data-item-id="<%= item.id %>"
                                    data-stock="<%= item.stock %>"
                                    data-quantity="<%= item.quantity %>"
                                    <%= item.isAvailable ? '' : 'disabled' %>>-</button>
                                
                                <span id="quantity-<%= item.id %>" class="w-10 text-center font-semibold text-lg"><%= item.quantity %></span>
                                
                                <button
                                    class="increase-quantity-btn w-8 h-8 rounded-full flex items-center justify-center bg-gray-700 text-lg text-white hover:bg-indigo-600 transition-colors"
                                    data-item-id="<%= item.id %>"
                                    data-stock="<%= item.stock %>"
                                    data-quantity="<%= item.quantity %>"
                                    <%= item.isAvailable ? '' : 'disabled' %>>+</button>
                                
                                <button class="remove-from-cart-btn w-8 h-8 rounded-full flex items-center justify-center bg-gray-700 text-gray-300 hover:bg-red-500 hover:text-white transition-colors ml-4" data-item-id="<%= item.id %>">
                                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" /></svg>
                                </button>
                            </div>
                        </div>
                    </div>
                <% }) %>
                </div>
            </div>

            <div class="lg:w-1/3">
                <div class="sticky top-8">
                    <div class="bg-gradient-to-br from-gray-900 to-gray-600 rounded-2xl p-8 shadow-xl">
                        <h2 class="text-2xl font-semibold mb-6 border-b border-gray-700 pb-4">Order Summary</h2>
                       <div class="space-y-4 text-gray-300">
    <div class="flex justify-between">
        <span>Subtotal (Original Price)</span>
        <span id="subtotal-value">₹<%= subtotal %></span>
    </div>
    
    <% if (totalDiscount > 0) { %>
        <div class="flex justify-between text-green-400">
            <span>Offer Discount</span>
            <span>- ₹<%= totalDiscount %></span>
        </div>
    <% } %>

    <div class="flex justify-between">
        <span>Tax (5%)</span>
        <span id="tax-value">₹<%= tax %></span>
    </div>
</div>
                        <div class="border-t border-gray-700 my-6"></div>
                        <div class="flex justify-between text-xl font-bold mb-6">
    <span>Total</span>
    <span id="total-value">₹<%= total %></span>
</div>
                        <button id="checkout-btn" class="w-full bg-gradient-to-br from-purple-900 to-purple-600 hover:bg-purple-700 text-white font-bold py-3 px-6 rounded-lg text-lg transition-all duration-300 transform hover:scale-105 shadow-lg shadow-indigo-500/30">
                            Proceed to Checkout
                        </button>
                        <a href="/user/product" class="block mt-6 text-center text-sm text-purple-700 hover:text-purple-500 transition-colors">← Continue Shopping</a>
                    </div>
                </div>
            </div>
        <% } else { %>
            <div class="w-full flex flex-col items-center justify-center text-center p-12">
                <img src="/images/cart.png" alt="A person pushing a shopping cart in an aisle" class="w-80 h-auto mb-6 rounded-lg shadow-lg" />
                <h2 class="text-3xl font-bold text-gray-200 mb-2">Your Cart is Empty</h2>
                <p class="text-gray-400 mb-6 max-w-md">Looks like your cart is waiting for some amazing products. Let's find something for you!</p>
                <a href="/" class="bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-3 px-8 rounded-lg transition-all duration-300 transform hover:scale-105 shadow-lg shadow-indigo-500/30">
                    Start Shopping
                </a>
            </div>
        <% } %>
    </div>
</div>

<style>
    @keyframes slideIn { from { transform: translateY(-100%); } to { transform: translateY(0); } }
    @keyframes fadeOut { from { opacity: 1; } to { opacity: 0; } }
    .animate-slideIn { animation: slideIn 0.3s forwards; }
    .animate-fadeOut { animation: fadeOut 0.5s 2.5s forwards; }
    .unavailable-item {
        opacity: 0.5;
    }
</style>

<script>
    const toastAlert = document.getElementById('toast-alert');
    const toastMessage = document.getElementById('toast-message');
    const successIcon = document.getElementById('toast-icon-success');
    const errorIcon = document.getElementById('toast-icon-error');
    const removeModal = document.getElementById('remove-modal');
    const confirmRemoveBtn = document.getElementById('confirm-remove-btn');
    const cancelRemoveBtn = document.getElementById('cancel-remove-btn');
    let timeoutId;
    let itemIdToRemove;
    function showAlert(type, message) {
        clearTimeout(timeoutId);
        toastMessage.textContent = message;
        toastAlert.classList.remove('bg-white', 'bg-green-500', 'bg-red-500');
        toastMessage.classList.remove('text-gray-900', 'text-white');
        successIcon.classList.add('hidden');
        errorIcon.classList.add('hidden');

        if (type === 'success') {
            toastAlert.classList.add('bg-green-500');
            toastMessage.classList.add('text-white');
            successIcon.classList.remove('hidden');
        } else if (type === 'error') {
            toastAlert.classList.add('bg-red-500');
            toastMessage.classList.add('text-white');
            errorIcon.classList.remove('hidden');
        }
        toastAlert.classList.remove('hidden', 'animate-fadeOut');
        void toastAlert.offsetWidth;
        toastAlert.classList.add('animate-slideIn');
        timeoutId = setTimeout(() => {
            toastAlert.classList.add('hidden');
        }, 3000);
    }
    async function updateCartQuantity(itemId, newQuantity) {
        try {
            const response = await fetch(`/user/cart/update-quantity/${itemId}`, {
                method: 'PATCH',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({ quantity: newQuantity }),
            });
            if (!response.ok) {
                console.error('Server response was not ok:', response.status, response.statusText);
                const errorData = await response.text();
                console.error('Server error response:', errorData);
                showAlert('error', 'Failed to update cart. Please try again.');
                return;
            }
            const data = await response.json();
            window.location.reload();
        } catch (error) {
            console.error('Network or parsing error:', error);
            showAlert('error', 'An unexpected error occurred. Please try again.');
        }
    }

    async function removeCartItem(itemId) {
        try {
            const response = await fetch(`/user/cart/remove-item/${itemId}`, {
                method: 'DELETE'
            });
            
            if (!response.ok) {
                console.error('Server response was not ok:', response.status, response.statusText);
                const errorData = await response.text();
                console.error('Server error response:', errorData);
                showAlert('error', 'Failed to remove item. Please try again.');
                return;
            }
            const data = await response.json();
            showAlert('success', 'Item removed from cart.');
            window.location.reload();
        } catch (error) {
            console.error('Network or parsing error:', error);
            showAlert('error', 'An unexpected error occurred. Please try again.');
        }
    }

    document.addEventListener('DOMContentLoaded', function() {
        const cartItems = <%- JSON.stringify(cartItems) %>;
        const message = <%- JSON.stringify(message || null) %>;
        if (message && message.text) {
            showAlert(message.icon, message.text);
        }
        const increaseQuantityBtns = document.querySelectorAll('.increase-quantity-btn');
        const decreaseQuantityBtns = document.querySelectorAll('.decrease-quantity-btn');
        const removeCartItemBtns = document.querySelectorAll('.remove-from-cart-btn');
        const checkoutBtn = document.getElementById('checkout-btn');
        const MAX_QUANTITY = 5;
        function checkCartForUnavailableItems() {
            return cartItems.some(item => !item.isAvailable);
        }
        if (checkoutBtn) {
            checkoutBtn.addEventListener('click', (event) => {
                event.preventDefault(); 
                if (checkCartForUnavailableItems()) {
                    showAlert('error', 'Please remove all unavailable items before proceeding to checkout.');
                } else {
                    window.location.href = '/user/checkout';
                }
            });
        }
        increaseQuantityBtns.forEach(button => {
            button.addEventListener('click', () => {
                const itemId = button.dataset.itemId;
                const quantitySpan = document.getElementById(`quantity-${itemId}`);
                const currentQuantity = parseInt(quantitySpan.textContent, 10);
                const maxStock = parseInt(button.dataset.stock, 10);
                const newQuantity = currentQuantity + 1;

                if (newQuantity <= maxStock && newQuantity <= MAX_QUANTITY) {
                    updateCartQuantity(itemId, newQuantity);
                } else {
                    showAlert('error', `Sorry, the selected quantity exceeds available stock or the maximum limit of ${MAX_QUANTITY}.`);
                }
            });
        });
        decreaseQuantityBtns.forEach(button => {
            button.addEventListener('click', () => {
                const itemId = button.dataset.itemId;
                const quantitySpan = document.getElementById(`quantity-${itemId}`);
                const currentQuantity = parseInt(quantitySpan.textContent, 10);
                const newQuantity = currentQuantity - 1;
                if (newQuantity >= 1) {
                    updateCartQuantity(itemId, newQuantity);
                } else {
                    showAlert('error', 'Quantity cannot be less than 1.');
                }
            });
        });
        removeCartItemBtns.forEach(button => {
            button.addEventListener('click', () => {
                itemIdToRemove = button.dataset.itemId;
                removeModal.classList.remove('hidden');
            });
        });
        if(confirmRemoveBtn) {
            confirmRemoveBtn.addEventListener('click', () => {
                if (itemIdToRemove) {
                    removeCartItem(itemIdToRemove);
                    removeModal.classList.add('hidden');
                }
            });
        }
        if(cancelRemoveBtn) {
            cancelRemoveBtn.addEventListener('click', () => {
                removeModal.classList.add('hidden');
                itemIdToRemove = null;
            });
        }
    });
</script>